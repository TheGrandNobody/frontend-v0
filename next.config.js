/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.creativecommons.org', 'www.softwarefreedom.org'],
  },
}

module.exports = nextConfig